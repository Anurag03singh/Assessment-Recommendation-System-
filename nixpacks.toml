[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3 -m ensurepip --upgrade", "python3 -m pip install -r backend/requirements-railway.txt"]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
